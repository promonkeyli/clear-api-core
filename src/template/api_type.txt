declare namespace API {
   {{range .}}
   type {{ .Name }} = {
   {{if .Name}}
      {{ range .Properties }}
         /** {{.Description}} */
         {{.Name}}?: {{.Type}}
      {{ end }}
   {{end}}
   }
   {{end}}
}