declare namespace API {
   {{- range $index, $value := . }}
   {{- if eq (len .Enum) 0 }}
    type {{ .Name }} = {
           {{- range .Properties }}
           {{- if .Description }}
           /** {{ .Description }} */
           {{- end }}
           {{ .Name }}
           {{- if eq .Required true -}}:{{- else }}?:{{- end -}}
           {{ .Type }};
           {{- end }}
    }
   {{- else }}
   type {{ .Name }} = {{ range $i,$v := .Enum }}
                      {{- $v }}
                      {{- if lt $i $value.EnumLastIndex }}
                      {{- " | " }}
                      {{- end }}
                      {{- end }};
   {{- end }}
   {{- end }}
}